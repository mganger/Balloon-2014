//Function that gets the required data from the sensors and puts it into
//the structure passed to it.
//Should include millis() to write time into data collection, so that we
//can record the collection time.

